class IntentUseCase1(val repository:IntentRepository) {
fun goIntent(context:Context, callbackOnCompleted:(Intent) - >Unit, 
callbackError:() - >Unit) {
if(repository.get1Intent().resolveActivity(context.packageManager) != null) {
          callbackOnCompleted(intent) 
           } else {
           callbackError() 
            }
}
} 
} 
